import { Box } from '@mui/material';
import React from 'react';

interface Todo {
  id: number;
  text: string;
  completed: boolean;
}
function TodoList() {
  return (
    <Box>list</Box>
  );
};

export default TodoList;